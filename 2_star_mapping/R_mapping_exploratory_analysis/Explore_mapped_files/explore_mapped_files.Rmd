---
title: "Untitled"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
install.packages("tidyverse")
library(tidyverse)
```

load splice junctions
```{r readdata}
headers <- c("CHR", "Intron_start", "Intron_end", "strand", "intron_motif", "annotated", "unique_mapped_reads", "multi-mapped reads", "max_overhang")
INF1 <- read.table("../../mappedfiles/INF1/E.elymi_Ee-Inf1_S14_L006_R1_001_SJ.out.tab")

colnames(INF1) <- headers
head(INF1)
```

```{r graph_splice_junctions}

toplot <- INF1 %>% group_by(annotated) %>% summarise( counts = n())  
toplot
ggplot(data=INF1, aes(x=))


```